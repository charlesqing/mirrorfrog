# Cloudflare Pages _redirects
# Format: <source> <target> <status>

# Trailing slash normalization for SEO
/docs/* /docs/:splat 200
/blog/* /blog/:splat 200

# Locale redirect (default to zh-Hans for international users)
/ /zh-Hans/ 302

# SPA fallback for client-side routing
/* /index.html 200
