# MirrorFrog 全量代码 Review 报告

- **日期**：2026-08-17
- **范围**：`src/`、`scripts/`、`docs/cards` 数据契约、配置文件（docusaurus/sidebars/vitest/package/tsconfig）
- **方法**：静态逐文件阅读 + `tsc --noEmit` / `vitest` 结果 + `chips.json` 字段覆盖率分析
- **结论性质**：**仅报告，0 文件修改**（与用户"review 所有代码"指令一致，未授权改动）

---

## 严重问题 (P0)

### 1. TCO 计算器冷却成本双重计算 ⚠️ 最高优先级
**位置**：`src/components/TcoCalculator.tsx` L647–651（计算）、L665–670（饼图）、L673–693（柱状图）

```ts
const annualDeviceElec   = tdpKW * qty * (idleRatio + (1 - idleRatio) * usage) * 8760 * price; // 设备 IT 电 = X
const annualFacilityElec = annualDeviceElec * pue;        // 设施总电 = X·PUE = 1.3X（已含冷却 0.3X）
const annualCooling      = annualDeviceElec * (pue - 1);  // 冷却 = X·(PUE-1) = 0.3X
const annualDc           = dcCost * qty;
const annualOp           = annualFacilityElec + annualDc + annualCooling;  // ← 又加了一遍冷却
```

**根因**：PUE 已把冷却能耗计入设施总电（`设施 = 设备 × PUE`）。再单独加 `annualCooling` 等于把冷却算了两次。

**影响量化**（默认 PUE=1.3）：
- 电费在 TCO 中被高估 **≈ +23%**（1.6X 实际有效电 vs 正确的 1.3X）
- 饼图"冷却"切片（0.3X）**完全是重复计入**，无对应真实成本
- `barData` 逐年的 `yrElec + yrCool` 同样双重计数 → 趋势图失真

**修复建议**（保持"设备电 + 冷却"两切片展示）：
```ts
const annualElec    = annualDeviceElec;            // 设备 IT 电 = X
const annualCooling = annualDeviceElec * (pue - 1);// 冷却 = 0.3X
const annualOp      = annualElec + annualDc + annualCooling; // = 1.3X + dc ✓
// pie: elec = annualElec * discountFactor; cool = annualCooling * discountFactor;
// bar:  yrElec = annualElec; yrCool = annualCooling;
```
（或直接删掉 `annualCooling`、电费用 `annualFacilityElec`，二选一，关键是不要两者相加）

---

## 高优问题 (P1)

### 2. TDP=0 芯片导致 TCO 电费归零（ascend-960 等整机系统）
**位置**：TcoCalculator.tsx L601、L644、L647
- `tdp: c.tdpW || 0` — 对 `tdpW=0` 无任何保护
- `tdpKW = chip ? chip.tdp / 1000 : 0` → `annualDeviceElec` 依赖 tdpKW → **tdp=0 时电费=0，TCO 只剩采购+租金**

**现状**：`chips.json` 中 `ascend-960` 的 `tdpW=0`（Atlas 960 为整机超级节点，MD 未给单卡 TDP）。现有缓解仅覆盖 `tdp>50000`（L732–737 显示"整机系统"警告），**tdp=0 无任何提示**。

**建议**：`tdpW` 为 0 或缺失时禁用"加入对比"/计算并提示"需提供单卡 TDP"。

### 3. compare.tsx 两列几乎全空 + fetch 静默失败
**位置**：`src/pages/compare.tsx`

**数据契约缺口**（chips.json 字段覆盖率，共 172 卡）：
| 字段 | 覆盖 | 备注 |
|---|---|---|
| specs.tdp | 171/172 | ✅ |
| specs.release | 172/172 | ✅ |
| specs.memory.capacity | 114/140 | 中 |
| specs.memory.bandwidth | 125/140 | 中 |
| specs.memory.type | **6/140** | ❌ 对比页"内存类型"列几乎全显 "—" |
| specs.compute.fp8 | **31/172** | ❌ "FP8"列几乎全空 |
| specs.compute.fp16 | 54/172 | 偏低 |

**问题**：
- 对比页依赖 `specs.memory.type` / `specs.compute.*`，覆盖率极低 → 多数列对绝大多数卡片显示 "—"，可用性差
- L59–67：fetch('/chips.json') 失败时仅 `setLoading(false)`，**无 error 状态 → 白屏**（首页 index.tsx 已有完整的 error+retry 模式可复用）

**建议**：高覆盖字段（显存容量/带宽）优先展示；覆盖率低的列降级或改为"—"；fetch 失败显示错误+重试。长期：在 `generate-data.mjs` 阶段补全 `memory.type`。

---

## 中优问题 (P2)

### 4. 陈旧魔法数字 + 多处"167 款"硬编码
- `src/pages/index.tsx` L425：`setPageCount(167 * 2 + 26 * 2 + 50)` — 167 芯片（现 **172**）、26 博客（现 **48**）→ meta 加载失败时的回退估算严重失真
- `src/data/home-content.zh.ts` L97：`查看完整对比表（167 款）→`
- `src/data/home-content.en.ts` L36：`View full comparison table (167 chips) →`
- `docusaurus.config.ts`：站点标题仍写 **"100+ 款"**（现 172）

**建议**：统一从 `static/meta.json`（已由 generate-data 生成）读取真实计数；或定义由 generate-data 注入的 `TOTAL_CHIPS` 常量。

### 5. 新增卡片未进 sidebars.ts（导航孤儿）
**位置**：`sidebars.ts`（手动枚举卡片）

08-18 新增的 3 张卡已存在于 `docs/cards/`：
- `docs/cards/huawei/ascend-960.md`
- `docs/cards/amd/mi350p.md`
- `docs/cards/amd/mi430x.md`

但 `sidebars.ts` 的「华为昇腾」「AMD Instinct」列表**均未包含**这 3 张卡。因侧栏为手动枚举（非 `autogenerated`），这些页面虽能通过 URL 访问（`onBrokenLinks:'warn'` 不报错），但**不在导航中显示，可发现性丢失**。

**建议**：在 sidebars.ts 对应厂商类别补列 `cards/huawei/ascend-960`、`cards/amd/mi350p`、`cards/amd/mi430x`；或将该层级改为 `autogenerated` 以免遗漏。

### 6. 脚本冗余：post-build.js / post-build.mjs + I18nTitle.tsx / config headTags
- `package.json` `postbuild: "node scripts/post-build.js"` → **post-build.mjs 从未被调用**（孤儿脚本），且二者功能重叠（均做 JSON-LD 注入 + sitemap 优化）
- `src/components/I18nTitle.tsx` 的客户端标题切换逻辑与 `docusaurus.config.ts` headTags 内联脚本重复

**建议**：删除 post-build.mjs（或合并进 post-build.js）；移除重复标题逻辑，保留一处。

---

## 低优 / 技术债 (P3)

### 7. `npm run typecheck` 永远失败（14 错误全在 post-build.mjs）
- `typecheck: "tsc"` 会扫描 `scripts/post-build.mjs` 的 ESM `import` → 报 TS1109/1127 语法错误（实为 ESM 在 tsc 配置下的**误报**，非真实 bug）
- `src/` 在 `strict: true` 下 **0 类型错误**；生产 `build` 不调用 tsc，故**不影响上线**
- 但 CI 中 `npm run typecheck` 恒失败 → 类型守卫形同虚设

**建议**：tsconfig 排除 `scripts/*.mjs`（或 `include` 仅限 `src/`），或在 typecheck 脚本加 `--noEmit` 并限定范围。

### 8. home-content 热门芯片为硬编码副本（DRY 违反）
`src/data/home-content.{zh,en}.ts` 的 `ZH_HOT_CHIPS` / `EN_HOT_CHIPS` 与 `chips.json` 重复规格（fp16Tflops、tdp 等），更新芯片需同步两处。注释已声明维护规范，但仍属脆弱点。

**建议**：首页热门改从 `chips.json` 派生（按 `vendorWeight × categoryWeight × fp16` 排序取 Top N），消除副本。

### 9. 工具脚本小问题
- `scripts/check-tables.mjs` L4：硬编码绝对路径 `D:/GitHub/mirrorfrog/docs/cards`（仅 Windows 可用，不可移植）
- `scripts/sync_tdp_to_md.py` / `add_missing_tdp.py`：依赖严格格式 `| **TDP** |`，对标题变体不鲁棒（与 generate-data 的 `KEY_MAP` 多标题支持不一致）
- `scripts/translate-blog.py`：发现 `<!-- truncate -->` 时直接改写源文件（有副作用，但已文档化）

---

## 已确认健康项 ✅
- **generate-data.mjs**：解析健壮（多标题锚定 `## 核心规格`、CRLF 兼容的字符串分割法、2/3 列识别、try-catch 跳过坏文件、`utf8` 编码合规）
- **chip-utils.ts / chip-utils.test.ts**：`rankScore`/`sortChips` 逻辑清晰，7/7 测试通过
- **vitest.config.ts / vitest.setup.ts**：配置标准，正确引入 `@testing-library/jest-dom` matchers
- **src/ 整体**：`strict` 模式下 0 类型错误
- **HomepageFeatures/index.tsx**：纯展示组件，无逻辑问题
- **package.json 校验链**：`prebuild`/`prestart` 自动跑 generate-data，数据层可靠

---

## 优先级行动清单
| 级别 | 问题 | 修复成本 | 影响 |
|---|---|---|---|
| **P0** | TCO 冷却双重计算 | 低（改 3 行 + 重算饼/柱） | 高：TCO 失真 23% |
| P1 | TDP=0 无保护 | 低 | 中：整机卡 TCO 归零 |
| P1 | compare 空列 + 静默失败 | 中 | 中：可用性 |
| P2 | 陈旧"167 款"硬编码 | 低 | 低：展示失真 |
| P2 | 新卡未进 sidebars | 低 | 中：可发现性 |
| P2 | 脚本/I18n 冗余 | 低 | 低：维护 |
| P3 | typecheck 恒失败 | 低 | 中：CI 失守 |
| P3 | home-content 副本 | 中 | 低：维护 |

> **最严重且最易修**：P0 冷却双重计算 —— 建议优先修复，单卡 1000W/默认 3 年估算下，电费+冷却项比真实值多出约 0.3× 设备电。
